# Log output format: "console" (colored, default) or "json" (structured)
MYLOGGER_FORMAT=console
# MYLOGGER_FORMAT=json

# Comma-separated list of log levels to exclude (optional)
# Available levels: debug, info, warning, error, critical
MYLOGGER_EXCLUDE=debug
#MYLOGGER_EXCLUDE=debug,info
